% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GridSearchSVR.R
\name{grid_signal_eval}
\alias{grid_signal_eval}
\title{Grid Signal Evaluation}
\usage{
grid_signal_eval(
  data_env,
  processed_data,
  bcv_folds = 5,
  norm_response_var,
  initial_column_values = c(1),
  grid_col_lags,
  lag_response = NULL,
  initial_prediction_value = 1,
  cost,
  nu,
  gamma = NULL,
  is_silent_mode = TRUE,
  should_plot_response = FALSE,
  generate_response_predictions_cv = FALSE,
  basic_filter_check = TRUE
)
}
\arguments{
\item{data_env}{Data environment containing datasets and parameters for
signal processing.}

\item{processed_data}{Preprocessed data used for generating predictions.}

\item{bcv_folds}{Number of folds for cross-validation, defaults to 5.}

\item{norm_response_var}{Normalized response variable used in the evaluation.}

\item{initial_column_values}{Numeric or vector providing initial values for
columns in the dataset, defaults to c(1).}

\item{grid_col_lags}{Integer or vector specifying column lags for the grid.}

\item{lag_response}{Integer or vector specifying the lag of the response,
defaults to NULL.}

\item{initial_prediction_value}{Numeric value indicating the initial value
for prediction, defaults to 1.}

\item{cost}{Numeric value representing the cost parameter for the SVR model.}

\item{nu}{Numeric value representing the nu parameter for the SVR model.}

\item{gamma}{Numeric value (optional) representing the gamma parameter for
the SVR model, defaults to NULL.}

\item{is_silent_mode}{Logical flag indicating if the function should operate
in silent mode, suppressing messages and output, defaults to TRUE.}

\item{should_plot_response}{Logical flag indicating whether the response
variable should be plotted, defaults to FALSE.}

\item{generate_response_predictions_cv}{Logical flag indicating if response
predictions should be generated, defaults to FALSE.}

\item{basic_filter_check}{Logical flag indicating if basic filters should
be applied, defaults to TRUE.}
}
\value{
A list containing the evaluation results, including signal quality
 metrics and potentially generated response predictions.
}
\description{
This function evaluates the quality of signals on a grid of parameters.
It generates signal predictions,
evaluates signal quality, performs cross-validation, and returns results
including advanced signal scores.
}
\examples{
\dontrun{
  grid_signal_eval(data_env, processed_data, ...)
}
}
